# 云端大脑 Brain 依赖
flask>=3.0.0
flask-socketio>=5.3.0
openai>=1.30.0
redis>=5.0.0
requests>=2.31.0
python-socketio>=5.11.0
websocket-client>=1.7.0

# QQ Bot 依赖（可选，需要 OneBot v11 时启用）
# python-socketio 已包含

# 微信依赖（可选）
# itchat  # 个人微信（已停止维护，不推荐）

# Telegram Bot（手机遥控）
# requests 已包含

# Agent 客户端 + 本地助手依赖（本地电脑安装）
python-socketio>=5.11.0
pyperclip>=1.8.0
pycaw>=20230407  # Windows 音量控制
Pillow>=10.0.0
pyautogui>=0.9.50  # 截图/键盘鼠标模拟
comtypes>=1.2.0  # pycaw 依赖
flask-socketio>=5.3.0  # 本地助手 Web 服务
