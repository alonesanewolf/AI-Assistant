# ==================== 核心依赖 ====================
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.11.0
openai>=1.30.0
requests>=2.31.0

# ==================== AI 模型 ====================
# DeepSeek（通过 openai 库调用）

# Ollama 本地模型（可选）
# 安装: https://ollama.com/

# ==================== 电脑操作 ====================
Pillow>=10.0.0
pyautogui>=0.9.50           # 截图/键盘鼠标模拟
pyperclip>=1.8.0            # 剪贴板读写

# ==================== 可选依赖 ====================
# 定时任务
schedule>=1.2.0

# 网页搜索
beautifulsoup4>=4.12.0

# 系统监控（心跳上报 CPU/内存）
psutil>=5.9.0

# Windows 音量控制（可选，安装较复杂）
# pycaw>=20230407
# comtypes>=1.2.0

# Redis 消息队列（仅云端 Brain 需要）
# redis>=5.0.0

# WebSocket 客户端（Agent 连接 Brain 需要）
websocket-client>=1.7.0

# 微信解密（企业微信消息加解密需要）
# pycryptodome>=3.20.0

# 桌面通知（可选）
# plyer>=2.1.0
